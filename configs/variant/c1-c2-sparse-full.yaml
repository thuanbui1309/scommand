# Phase 05 C1+C2 + Phase 06 sparsity — hero config combining all three.
model:
  long_range_branch: spiking_mamba

neuron:
  type: plif

loss:
  sparsity:
    enabled: true
    lam: 1.0e-3
    target_fr: null

training:
  grad_clip: 1.0
